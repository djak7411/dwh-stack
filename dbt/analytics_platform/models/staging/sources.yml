version: 2

sources:
  - name: iceberg
    schema: analytics  
    meta:
      type: iceberg
    tables:
      - name: customers
        identifier: iceberg_customers
        description: "Customers table from Iceberg"
      - name: orders
        identifier: iceberg_orders
        description: "Orders table from Iceberg"